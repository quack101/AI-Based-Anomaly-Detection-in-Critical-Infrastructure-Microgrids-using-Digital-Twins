"""
services package — Business logic and external integrations.

Modules:
    data_ingestion   — CSV dataset loader (Track A)
    twin_engine      — Digital Twin baseline + residual engine (Track A)
    supabase_client  — Supabase connection singleton (Track B)
    logger           — Anomaly event writer to Supabase (Track B)
"""
